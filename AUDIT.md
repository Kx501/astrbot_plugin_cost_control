# 代码审查与修复记录

审查日期：2026-09-06。范围为插件入口、全部后端模块、React 面板、静态构建产物、价格生成脚本和测试。运行时接口依据本地 AstrBot 4.25.5 源码核对；未连接正在运行的 Bot，也未调用付费模型或改动真实业务数据库。

以下为本轮已复现并修复的问题。优先级 P1 表示影响计费、预算、配置持久化或主要功能，P2 表示诊断、边界处理、兼容性或资源管理问题。此记录描述本轮审查结果，不代表对任意部署环境不存在缺陷的保证。

| 编号 | 优先级 | 已确认的问题 | 修复方式与主要文件 |
|---|---|---|---|
| 01 | P1 | 总开关和平台范围没有约束请求钩子 | 三个主钩子统一检查；停用后定时报告与价格同步停止执行。`main.py`、`schedule.py` |
| 02 | P1 | 同会话并发和未采样请求使用上一轮归因 | 按 event 持有当前采样；保留会话最近样本供查询；限制未完成请求快照数量。`attributor.py`、`main.py` |
| 03 | P1 | 响应到达前切换 Provider/模型会记错账户和价格 | 请求尾部固化 Provider 和请求指定模型；响应使用该快照。`supplement.py` |
| 04 | P1 | DeepSeek 缓存命中仍按普通输入价计费，命中率分母重复计算 | 在总输入守恒的前提下补齐缓存/非缓存分量，普通与降级采集复用。`supplement.py`、`budget.py` |
| 05 | P2 | 字典响应无法解析缓存；一个坏字段掩盖其他有效用量 | SDK 对象与字典统一读取，各字段独立解析；响应 ID 可从 raw 回退。`supplement.py` |
| 06 | P2 | 工具 schema、消息角色、tool_calls 和长描述变化漏诊；System Prompt 增删漏诊 | 完整结构签名独立于裁切后的展示文本，覆盖空值变化；归因估算纳入参数 schema 和工具调用参数。`cache_diag.py`、`attributor.py` |
| 07 | P1 | 跨货币继承手工价/分时价时直接复制数字 | 继承字段按配置汇率换算，价格上下文携带汇率。`cost.py`、`config.py` |
| 08 | P1 | 价格源没有缓存读价时被算成免费 | 缺失回退普通输入价，显式 0 仍为免费。`cost.py` |
| 09 | P1 | 免费基础价上的付费服务档被丢弃 | 保留不能表达为倍率的绝对单价，并正确叠加聚类倍率。`cost.py` |
| 10 | P1 | 后端和前端丢弃服务档显式 0 倍率；空白聚类倍率被算成 0 | 服务档保留 0；聚类空白恢复默认 1，非法值拒绝保存。`config.py`、定价编辑组件 |
| 11 | P1 | 阶梯价和表达式先汇总 Token 后计算，跨调用错误进入高价档 | 对非线性模式读取原生明细逐条计算，保留调用时刻；线性模式继续 SQL 聚合。`usage_query.py` |
| 12 | P1 | 函数参数里的三元表达式错误转译；隐藏分支未知变量未发现 | 按参数边界转译，静态校验所有变量。`expr_eval.py` |
| 13 | P1 | 动态分支可能返回负成本、NaN、Inf；外部参数字符串绕过长度限制 | 运行时拒绝非法结果，对参数和请求头字符串同样实施资源限制。`expr_eval.py` |
| 14 | P1 | 时间表达式不识别 ISO 字符串，缺少时间时用当前时刻改写历史费用 | 支持 ISO 时间，缺失时间明确失败，查询保留原始调用时刻。`expr_eval.py`、`usage_query.py` |
| 15 | P1 | 非有限汇率、非规范代码或错误 USD 基准污染换算 | 过滤非法汇率、规范代码，USD 基准固定为 1。`exchange_rates.py` |
| 16 | P1 | 非法星期配置退化为每天生效 | 非法分时策略拒绝保存，运行时跳过损坏条目。`pricing_schedule.py` |
| 17 | P1 | 价格源 ID 前缀相同会相互误删；损坏状态计数使有效目录失效 | 按真实 source 身份替换价格，状态字段独立容错。`price_catalog.py` |
| 18 | P2 | 一个价格源抛异常中断全部同步 | 单源隔离异常，成功结果继续保存，失败保留旧目录。`price_sources.py` |
| 19 | P1 | 价格生成脚本把显式免费缓存价改成输入价 | 区分缺失和 0，拒绝非有限价格。`scripts/sync_pricing.py` |
| 20 | P1 | 全局 fallback 忽略配置的备用 Provider 库 | 按启用顺序接入全局超限处理。`budget.py` |
| 21 | P1 | Provider 内部 TypeError 触发第二次付费调用 | 调用前检查签名，单次调用，不在运行时异常后盲目重试。`budget.py` |
| 22 | P1 | 降级调用未写原生统计；空文本响应漏记；错误响应当成功 | 每次完成响应落账，补写原生 ProviderStat，正确识别错误响应，失败计价保留 NULL。`budget.py` |
| 23 | P1 | 首条不限额局部规则仍继续全局预算检查 | 按文档的“首条匹配替代全局”语义立即放行。`budget.py` |
| 24 | P2 | 降级历史超出截断上限，图片/音频和额外用户内容丢失 | 严格截断历史并传递 Provider 支持的媒体参数。`budget.py` |
| 25 | P1 | 用户统计缺时间过滤、按输入而非总 Token 排序、包含空用户 | SQL 使用窗口条件和总量排序，排除无用户记录。`store.py` |
| 26 | P1 | 用户预算忽略历史固化金额，修改定价改写过去用量 | 普通模式优先使用成本快照并换算主货币，按请求模式保留去重语义。`store.py` |
| 27 | P1 | 并发通知重复发送；发送失败占用冷却 | 会话锁覆盖检查、发送、标记；成功后才记冷却。`notifier.py` |
| 28 | P1 | 建表完成前发布数据库连接；初始化失败无法恢复；旧缓存表缺列 | 初始化锁、完成后发布、失败释放连接并可重试；补齐旧表迁移。`store.py` |
| 29 | P2 | 夏令时窗口可能落在未来；时序 Python 分桶与 SQL 时区不一致 | 以 UTC 比较边界，分桶统一 UTC。`budget.py`、`usage_query.py` |
| 30 | P1 | 报表静默截断到 5000 条且多个查询结束时间不同 | 全窗口读取并固定同一个结束时间。`analytics.py` |
| 31 | P1 | `/budget` 和 AI 诊断直接给原币阈值贴主货币符号 | 阈值先换算主货币再展示、比较。`commands.py`、`ai_diag.py` |
| 32 | P1 | AI 局部花费恒为 0；部分预算被 prompt 截断 | 计算主体最大花费，失败标记未知；完整保留预算数据并标记用户取样范围。`ai_diag.py` |
| 33 | P2 | `/cost` 分钟分组重复模型，单行异常使整条命令失败 | 逐行容错、模型合并、按成本排序；明确提示不完整金额。`commands.py` |
| 34 | P2 | AI 返回列表/错类型字段击穿后端或前端；坏缓存时间抛异常 | 校验结论与缓存结构；无效缓存安全回退；使用真实模型元数据。`ai_diag.py` |
| 35 | P1 | 默认配置浅拷贝相互污染；部分配置把缺省 True/时间改成 False/None | 深复制合并、连续覆盖正确处理；缺失值恢复真实默认，字符串布尔正确解析。`config.py` |
| 36 | P1 | 并发写 config.json 共享临时文件，写盘相互破坏 | 每次创建独立临时文件，原子替换并清理失败文件，拒绝 JSON 非有限数。`config.py` |
| 37 | P1 | 无效价格被静默归零/删除，接口仍声称保存成功 | 写入端严格校验模式、表达式、数值、阶梯结构与分时覆盖；本地加载保留容错。`web_api.py`、`config.py` |
| 38 | P1 | 自动保存期间的新修改被当成已保存，切页漏写 | 保存独立快照、串行排空最新值，卸载登记写请求，flush 等待完成并传播错误。`useAutoSave.ts`、`autoSaveQueue.ts` |
| 39 | P1 | 旧 GET 覆盖新配置，切页读取发生在保存之前 | 配置读写队列与版本保护，读取等待写入，检测交叉修改后重新读取。`api.ts`、预算/定价页面 |
| 40 | P1 | 日报/平台字段写错路径，旧整份配置覆盖其他页修改 | 正确绑定 alerts/platforms，仅提交本页编辑项，固定配置组保留兄弟字段。`SettingsView.tsx`、`web_api.py` |
| 41 | P1 | 价格源开关并发读改写丢失修改 | 将读取与修改放进同一个写入队列，并保留已有认证配置。`api.ts` |
| 42 | P1 | 清空按钮缺确认参数、非法模块会部分删除、重复并发清空 | 补确认字段；执行前整体验证模块，去重并阻止重复并发。`api.ts`、`web_api.py` |
| 43 | P1 | 汇率同步写盘失败仍返回成功并修改运行时状态 | 先完成持久化，失败返回错误；页面保留未保存设置。`web_api.py`、`SettingsView.tsx` |
| 44 | P1 | 会话聚合费用固定为 0 | 使用每个会话实际定价数据计算。`web_api.py` |
| 45 | P2 | 明细日期按 UTC 截切、刷新跨日失效、环比边界重复 | 浏览器本地日界转 UTC，刷新重算窗口，排除相邻窗口重叠。`recordRange.ts`、`web_api.py` |
| 46 | P1 | New API 源使用模型短名，供应商同名时相互覆盖 | 新源使用完整 Provider ID；已有源按 URL 复用。`PricingView.tsx` |
| 47 | P2 | 旧币种符号导致选择器空值或新价格草稿币种错误 | 显示和草稿统一转换为货币代码。前端格式化与定价组件 |
| 48 | P2 | `/report` 测试的同步 mock 导致实际 handler 失败却测试通过 | 改为异步 mock，并检查真实返回结果；新增真实 ReactDOM 生命周期回归。测试目录 |
| 49 | P2 | 安装声明最低版本与依赖要求不一致 | README、metadata 与 pyproject 统一要求 AstrBot >=4.25.5。 |
| 50 | P1 | 工具循环结束钩子只有最终一轮用量，按用户预算漏记前面轮次 | 在 AstrBot 4.25.5 兼容路径读取同一 event 的累计统计，严格核对对象身份；累计 Token 与最终响应 raw 范围分别标注。`supplement.py` |
| 51 | P2 | 卸载后 Cron 回调继续持有旧插件，数据库连接池没有释放 | 显式 terminate，按本实例 job ID 撤销任务，关闭连接池并防止滞后回调重新打开。`main.py`、`schedule.py`、`store.py` |

## 验证与部署边界

- Python：`.venv/bin/pytest -q`，576 项全部通过（基线 427 项，本轮新增 149 项）。
- 前端：`npm test`，20 项全部通过；`npm run build:check` 的 TypeScript 检查和 Vite 构建通过。
- 静态检查：`.venv/bin/ruff check .` 与 `git diff --check` 通过。
- 回归包含临时 SQLite 真查询、迁移与并发，以及入口钩子、跨币种、动态表达式、保存失败和前后端配置协议。
- 前端测试包括真实 ReactDOM/jsdom 挂载的自动保存生命周期和 SettingsView；TypeScript 检查与 Vite 构建通过，`pages/dashboard` 已同步构建。
- 构建保留已有 bridge-sdk 外部脚本与单包体积提示，不影响产物生成。
- 所有修改保留在工作区，未自动提交、发布或重启生产 Bot。
- AstrBot 4.25.5 的一条原生统计代表一次 agent 执行，可能包含多次工具循环模型调用。累计 Token 的兼容读取依赖受保护的宿主私有注册表；接口不存在时回退响应采样。完整逐轮成本和真实调用次数仍无法从最终响应恢复。
- 原生 ProviderStat 缺少 request_id、逐轮缓存创建量和完整响应计费上下文。以该表为源的统计仍有明确的数据粒度限制；历史未记录字段无法从现有数据精确恢复。
- `per_request` 的补充聚合按请求去重，单条明细与原生聚合仍沿用现有近似语义。当前定价重新解释原生历史费用、按当前汇率展示历史金额也是现有产品行为。
- 会话费用查询仍按会话逐个查询，海量数据报表使用全量读取；正确性已修，超大数据库的分页/流式性能优化未混入本轮修复。
- 预算在请求前检查已发生用量，不预估或预留在途请求费用；并发请求和单次超大调用可能在结算后超过上限。
